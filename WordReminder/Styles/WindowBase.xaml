<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:WordReminder.Controls">

    <Style TargetType="{x:Type controls:WindowBase}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:WindowBase}">
                    <Border x:Name="WindowBorder"
                            Background="{StaticResource SurfaceAltBrush}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16"
                                              ShadowDepth="2"
                                              Opacity="0.15"
                                              Color="#000000"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Title bar -->
                            <Border x:Name="PART_TitleBar"
                                    Grid.Row="0"
                                    Background="{StaticResource PrimaryBgBrush}"
                                    CornerRadius="8,8,0,0"
                                    Cursor="Arrow">
                                <Grid>
                                    <TextBlock Text="{TemplateBinding TitleText}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource PrimaryDarkBrush}"
                                               VerticalAlignment="Center"
                                               Margin="16,0,0,0"/>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Border x:Name="PART_MinimizeButton"
                                                Width="36" Height="36"
                                                Background="Transparent"
                                                Cursor="Hand">
                                            <TextBlock Text="&#x2014;"
                                                       FontSize="14"
                                                       Foreground="#888888"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                        <Border x:Name="PART_CloseButton"
                                                Width="36" Height="36"
                                                Background="Transparent"
                                                Cursor="Hand">
                                            <TextBlock Text="&#x2715;"
                                                       FontSize="12"
                                                       Foreground="#888888"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Content area -->
                            <ContentPresenter Grid.Row="1"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger SourceName="PART_MinimizeButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_MinimizeButton" Property="Background"
                                    Value="#E5E5E5"/>
                        </Trigger>
                        <Trigger SourceName="PART_CloseButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_CloseButton" Property="Background"
                                    Value="{StaticResource CloseButtonHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
