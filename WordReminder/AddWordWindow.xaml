<Window x:Class="WordReminder.AddWordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加单词"
        Width="350" MaxWidth="350"
        Height="180" MaxHeight="180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="请输入英文单词:" Margin="0,0,0,10"/>

        <TextBox x:Name="WordInput"
                 Grid.Row="1"
                 Height="30"
                 VerticalContentAlignment="Center"
                 Padding="5,0"
                 KeyDown="WordInput_KeyDown"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="确定"
                    Width="70"
                    Height="25"
                    Margin="0,0,10,0"
                    Click="Ok_Click"
                    IsDefault="True"/>
            <Button Content="取消"
                    Width="70"
                    Height="25"
                    Click="Cancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>