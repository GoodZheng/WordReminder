<controls:WindowBase x:Class="WordReminder.Views.AddWordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WordReminder.Controls"
        TitleText="添加单词"
        Width="350"
        Height="180">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="请输入英文单词:" Margin="0,0,0,10"/>

        <TextBox x:Name="WordInput"
                 Grid.Row="1"
                 Height="30"
                 VerticalContentAlignment="Center"
                 Padding="5,0"
                 Text="{Binding WordText, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="确定"
                    Margin="0,0,10,0"
                    Command="{Binding OkCommand}"
                    IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="取消"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</controls:WindowBase>
