<controls:WindowBase x:Class="WordReminder.Views.TranslationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:WordReminder.Converters"
        xmlns:controls="clr-namespace:WordReminder.Controls"
        TitleText="万能翻译"
        Width="600"
        Height="500"
        CanResize="True"
        MinWidth="500"
        MinHeight="400">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

        <!-- 单词详情上下文菜单 -->
        <ContextMenu x:Key="WordContextMenu" Style="{StaticResource FluentContextMenuStyle}">
            <MenuItem x:Name="AddWordMenuItem"
                      Header="加入单词列表"
                      Style="{StaticResource FluentMenuItemStyle}"
                      Click="AddWordMenuItem_Click"/>
        </ContextMenu>

        <!-- 反馈标签显示/隐藏动画 -->
        <Storyboard x:Key="FeedbackShowStoryboard">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                <DoubleKeyFrame KeyTime="0:0:0" Value="0.9"/>
                <DoubleKeyFrame KeyTime="0:0:1.2" Value="0.9"/>
                <DoubleKeyFrame KeyTime="0:0:1.5" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 输入区域 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="请输入要翻译的文本（支持中英文互翻）" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/>
            <TextBox Height="60"
                     Name="InputTextBox"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="14"
                     Padding="8"
                     PreviewKeyDown="InputTextBox_PreviewKeyDown"
                     Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- 翻译按钮 -->
        <Button Grid.Row="1"
                Content="翻译"
                Width="100"
                Height="32"
                HorizontalAlignment="Left"
                Command="{Binding TranslateCommand}"
                IsEnabled="{Binding IsTranslating, Converter={StaticResource InverseBooleanConverter}}"
                Margin="0,0,0,10" Style="{StaticResource PrimaryButtonStyle}"/>

        <!-- 结果区域 -->
        <Border Grid.Row="2"
                BorderBrush="{StaticResource PrimaryBgBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Background="{StaticResource SurfaceAltBrush}"
                VerticalAlignment="Stretch">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalAlignment="Stretch">
                <StackPanel>
                    <!-- 加载提示 -->
                    <TextBlock Text="{Binding LoadingText}"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="14"
                               Visibility="{Binding ShowLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <!-- 错误消息 -->
                    <TextBox Text="{Binding ErrorMessage, Mode=OneWay}"
                             Foreground="{StaticResource DangerBrush}"
                             TextWrapping="Wrap"
                             FontSize="14"
                             TextAlignment="Justify"
                             IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             Padding="0"
                             VerticalAlignment="Top"
                             Visibility="{Binding ShowError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <!-- 翻译结果 -->
                    <ContentControl Content="{Binding TranslationResult}" Visibility="{Binding TranslationResult, Converter={StaticResource NullToVisibilityConverter}}">
                        <ContentControl.ContentTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <!-- 错误情况 -->
                                    <TextBox Text="{Binding TranslatedText, Mode=OneWay}"
                                             Foreground="{StaticResource DangerBrush}"
                                             TextWrapping="Wrap"
                                             FontSize="14"
                                             TextAlignment="Justify"
                                             IsReadOnly="True"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             Padding="0"
                                             VerticalAlignment="Top"
                                             Visibility="{Binding IsError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 原文 -->
                                    <TextBlock Text="原文" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,15,0,8" Visibility="{Binding Text, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    <TextBox Text="{Binding Text, Mode=OneWay}"
                                             TextWrapping="Wrap"
                                             FontSize="14"
                                             Margin="0,0,0,8"
                                             Foreground="{StaticResource TextPrimaryBrush}"
                                             TextAlignment="Justify"
                                             IsReadOnly="True"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             Padding="0"
                                             VerticalAlignment="Top"
                                             VerticalScrollBarVisibility="Disabled"/>

                                    <!-- 英文单词翻译为中文 - 译文 -->
                                    <TextBlock Text="译文" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,5" Visibility="{Binding IsEnToZhWord, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <TextBox Text="{Binding TranslatedText, Mode=OneWay}"
                                             TextWrapping="Wrap"
                                             FontSize="15"
                                             FontWeight="Bold"
                                             Margin="0,0,0,15"
                                             Foreground="{StaticResource PrimaryBrush}"
                                             TextAlignment="Justify"
                                             IsReadOnly="True"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             Padding="0"
                                             VerticalAlignment="Top"
                                             VerticalScrollBarVisibility="Disabled"
                                             Visibility="{Binding IsEnToZhWord, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 英文句子翻译为中文 - 译文 -->
                                    <TextBlock Text="译文" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,5" Visibility="{Binding IsEnToZhSentence, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <TextBox Text="{Binding TranslatedText, Mode=OneWay}"
                                             TextWrapping="Wrap"
                                             FontSize="15"
                                             FontWeight="Bold"
                                             Margin="0,0,0,10"
                                             Foreground="{StaticResource PrimaryBrush}"
                                             TextAlignment="Justify"
                                             IsReadOnly="True"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             Padding="0"
                                             VerticalAlignment="Top"
                                             VerticalScrollBarVisibility="Disabled"
                                             Visibility="{Binding IsEnToZhSentence, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 分割线（句子翻译时显示） -->
                                    <Border Height="1" Background="#E0E0E0" Margin="0,5,0,15" Visibility="{Binding IsEnToZhSentence, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 难词解释标题（句子翻译时显示） -->
                                    <TextBlock Text="难词解释" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" Visibility="{Binding IsEnToZhSentence, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 单词详情 -->
                                    <ItemsControl ItemsSource="{Binding WordDetails}" Margin="0,0,0,10">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="#DCDCDC" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,10" Background="White">
                                                    <Grid>
                                                        <!-- 添加成功反馈标签 -->
                                                        <Border x:Name="FeedbackBadge"
                                                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                                                Background="{StaticResource SuccessBrush}"
                                                                CornerRadius="10" Padding="8,4"
                                                                Visibility="Collapsed"
                                                                Opacity="0.9"
                                                                Panel.ZIndex="1">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="✓" Foreground="White" FontSize="12" FontWeight="Bold" Margin="0,0,4,0"/>
                                                                <TextBlock Text="已添加" Foreground="White" FontSize="11"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <StackPanel>
                                                            <!-- 单词名称 + 音标（双击弹出菜单） -->
                                                            <TextBlock FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"
                                                                       MouseLeftButtonDown="WordName_MouseLeftButtonDown"
                                                                       Cursor="Hand">
                                                                <Run Text="{Binding Word, Mode=OneWay}"/>
                                                                <Run Text="{Binding Phonetic, Mode=OneWay}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                                                            </TextBlock>

                                                        <!-- 词性 + 释义 -->
                                                        <TextBox Text="{Binding DisplayDefinition, Mode=OneWay}"
                                                                 FontSize="14"
                                                                 Margin="0,5,0,0"
                                                                 TextWrapping="Wrap"
                                                                 TextAlignment="Justify"
                                                                 IsReadOnly="True"
                                                                 BorderThickness="0"
                                                                 Background="Transparent"
                                                                 Padding="0"
                                                                 VerticalAlignment="Top"/>

                                                        <!-- 例句 -->
                                                        <TextBox Text="{Binding Example, Mode=OneWay}"
                                                                 FontSize="12"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 FontStyle="Italic"
                                                                 TextWrapping="Wrap"
                                                                 Margin="0,8,0,0"
                                                                 TextAlignment="Justify"
                                                                 IsReadOnly="True"
                                                                 BorderThickness="0"
                                                                 Background="Transparent"
                                                                 Padding="0"
                                                                 VerticalAlignment="Top"
                                                                 Visibility="{Binding Example, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                        <TextBox Text="{Binding ExampleTranslation, Mode=OneWay}"
                                                                 FontSize="12"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 TextWrapping="Wrap"
                                                                 Margin="0,2,0,0"
                                                                 TextAlignment="Justify"
                                                                 IsReadOnly="True"
                                                                 BorderThickness="0"
                                                                 Background="Transparent"
                                                                 Padding="0"
                                                                 VerticalAlignment="Top"
                                                                 Visibility="{Binding ExampleTranslation, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- 中文句子翻译为英文 -->
                                    <TextBlock Text="多种翻译" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,8" Visibility="{Binding IsZhToEn, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 翻译选项 -->
                                    <ItemsControl ItemsSource="{Binding Options}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="{StaticResource PrimaryBgBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,12" Background="{StaticResource SurfaceAltBrush}">
                                                    <StackPanel>
                                                        <TextBox Text="{Binding Text, Mode=OneWay}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{StaticResource PrimaryBrush}" TextWrapping="Wrap" TextAlignment="Justify" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" VerticalAlignment="Top"/>
                                                        <TextBox Text="{Binding Scenario, Mode=OneWay}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic" Visibility="{Binding Scenario, Converter={StaticResource StringToVisibilityConverter}}" TextWrapping="Wrap" TextAlignment="Justify" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" VerticalAlignment="Top" Margin="0,0,0,5"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ContentControl.ContentTemplate>
                    </ContentControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 底部状态栏 -->
        <TextBlock Grid.Row="3" Text="{Binding StatusText}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/>
    </Grid>

</controls:WindowBase>
